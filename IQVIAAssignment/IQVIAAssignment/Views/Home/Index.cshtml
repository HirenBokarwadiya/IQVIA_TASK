@{
    ViewBag.Title = "Home Page";
}
<script src="~/Scripts/jquery.validate.min.js"></script>
@*<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.4.1/js/bootstrap-datepicker.min.js"></script>*@
<script src="~/Scripts/datepicker.js"></script>
@*<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.4.1/css/bootstrap-datepicker3.css" />*@
<link href="~/Content/datepicker.css" rel="stylesheet" />
@*@using (Html.BeginForm())
    {*@


<div class="form-horizontal">
    <div class="bootstrap-iso">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12 col-sm-12 col-xs-12">
                    <form method="post" id="form1">
                        <div class="form-group">
                            <label class="control-label" for="date"> Start Date</label>
                            <input class="form-control" id="txtFromDate" name="txtFromDate" placeholder="YYYY/MM/DD" type="text" autocomplete="off" />
                        </div>
                        <div class="form-group">
                            <label class="control-label" for="date">End Date</label>
                            <input class="form-control" id="txtToDate" name="txtToDate" placeholder="YYYY/MM/DD" type="text" autocomplete="off" />
                        </div>
                        <div class="form-group">
                            <button class="btn btn-primary " name="submit" type="submit">Submit</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

</div>


@*}*@

<script>
    $(document).ready(function () {
        $("#form1").validate({
            rules: {
                txtFromDate: "required",
                txtToDate:"required"
            },
            messages: {
                txtFromDate: "Please enter valid date",
                txtToDate:"Please enter valida date"
            }
        })
    });
    $(function () {

        $("#txtFromDate").datepicker({
            //orientation: "bottom auto",
            format: 'yyyy-mm-dd',
            autoclose: true,
            onSelect: function (selected) {
                $("#txtToDate").datepicker("option", "minDate", selected)
            }
        });
        $("#txtToDate").datepicker({
            //orientation: "bottom auto",
            format: 'yyyy-mm-dd',
            autoclose: true,            
            onSelect: function (selected) {
                $("#txtFromDate").datepicker("option", "maxDate", selected)
            }
        });
    });
</script>

